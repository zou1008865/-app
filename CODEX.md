# CODEX.md

## 1. 项目目标

本项目是一个面向大学生个人使用的人民币收支管理 App。

当前目标是：记录支出和收入，查看本月收入、本月支出、本月结余，按分类理解钱从哪里来、花到哪里去，并能查看完整收支明细。

这是一个 Vibecoding 学习项目，后续开发优先选择容易理解、容易维护的方案，并在每次小阶段完成后进行验证。

## 2. 当前实现概览

项目目前已经从“单页面支出记录工具”升级为“多页面个人收支管理 App”，并且已经加入本机数据库接口。

当前支持：

- 支出记录。
- 收入记录。
- 本月收入、本月支出、本月结余。
- 收入和支出分类统计。
- 分类详情页面。
- 收支明细按月份、收入/支出、分类筛选。
- 固定分类和自定义分类。
- 自定义分类新增、改名、删除。
- 删除自定义分类时，将历史记录迁移到另一个分类。
- 电脑端侧边导航，窄屏下改为顶部导航。
- 旧版浏览器数据迁移到本机数据库。

当前暂不支持：

- 账户、银行卡、钱包。
- 初始余额。
- 预算。
- 转账。
- 登录。
- 云同步。
- 多用户和权限管理。
- 导入、导出账本文件。

这些能力以后如果要增加，必须先列出方案并等待用户确认。

## 3. 技术栈和运行方式

| 技术 | 新手解释 | 用途 |
| --- | --- | --- |
| React | 搭建网页界面的工具 | 制作页面、表单、列表和统计区域 |
| React Router | 根据网址切换页面的工具 | 管理 `/`、`/transactions`、`/categories` 等路由 |
| TypeScript | 更严格的 JavaScript | 减少数据字段写错和类型混乱 |
| Vite | 前端启动和打包工具 | 本地预览和生成正式网页文件 |
| Express | 编写本机 HTTP 接口的工具 | 接收前端的查询、保存、删除请求 |
| sql.js | 可以在 Node.js 中使用的 SQLite 数据库 | 保存本机账本数据 |
| CSS | 控制网页样式的语言 | 实现校园清新、现代效率工具风格 |
| localStorage | 浏览器提供的本地存储 | 兼容和读取旧版浏览器账本，作为迁移来源 |

当前的数据主存储是项目目录下的：

```text
data/finance.db
```

前端通过 `/api` 请求访问 Express 接口。Vite 开发服务器运行在 `5173` 端口，并把 `/api` 请求转发到 `5174` 端口的本机 API 服务。

启动开发环境：

```bash
npm install --cache .npm-cache
npm run dev
```

`npm run dev` 会同时启动前端和 API 服务。通常在浏览器打开：

```text
http://127.0.0.1:5173/
```

如果只启动 Vite 前端而没有启动 API 服务，页面无法读取或保存账本数据。

正式构建检查：

```bash
npm run build
```

这个命令会检查 TypeScript，并使用 Vite 生成 `dist` 目录。它不会替代开发服务器，也不会自动启动 API 服务。

## 4. 页面结构和导航

| 路由 | 页面 | 主要内容 |
| --- | --- | --- |
| `/` | 财务概览 | 本月收入、本月支出、本月结余、最近记录、分类简报、新增入口 |
| `/transactions` | 收支明细 | 全部记录，支持月份、收入/支出、分类筛选和删除 |
| `/categories` | 分类统计 | 分别展示收入分类和支出分类的金额与占比 |
| `/categories/:type/:category` | 分类详情 | 查看某个收入或支出分类在指定月份的全部记录 |
| `/category-manager` | 分类管理 | 管理自定义分类；固定分类不可修改 |

电脑端使用侧边导航，屏幕较窄时使用顶部横向导航。

## 5. 前端和后端数据流

页面加载时：

1. `App` 读取浏览器中可能存在的旧版数据。
2. App 调用 `/api/migrate/local-storage`，尝试把旧数据迁移到本机数据库。
3. 各个页面调用 `/api/state`，读取本机数据库中的收支记录和自定义分类。
4. 新增、删除、分类管理操作通过 API 写入数据库。
5. API 写入成功后，页面更新自己的 React 状态，因此用户可以立即看到结果。

数据库由 `server/index.ts` 初始化和管理：

- `transactions` 表保存收支记录。
- `custom_categories` 表保存自定义分类。
- `app_metadata` 表保存迁移完成标记等应用信息。
- 写入操作使用临时文件替换方式保存数据库，减少写入过程中损坏文件的风险。
- 多个写入请求通过队列依次处理，避免同时写数据库。

## 6. Transaction 数据模型

项目使用 `Transaction` 表示一条收支记录，不再使用旧版的 `Expense` 作为主要模型。

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| `id` | 记录唯一标识 | 新增时生成，不能重复 |
| `type` | 收支类型 | `expense` 或 `income` |
| `amountInCents` | 金额，单位是分 | 必须是大于 0 的整数 |
| `category` | 分类 | 必须属于对应类型的固定分类或自定义分类 |
| `note` | 备注 | 可以为空 |
| `date` | 发生日期 | 新增时默认今天，用户可以修改 |
| `createdAt` | 创建时间 | 新增记录时自动生成 |

金额规则：

- 输入时最多允许两位小数，例如 `12`、`12.5`、`12.50`。
- 保存时转换为整数“分”，例如 `12.50` 保存为 `1250`。
- 金额必须大于 0。
- 统计时使用整数分计算，展示时再格式化为人民币金额。

## 7. 分类设计

固定支出分类：

- 餐饮。
- 交通。
- 学习。
- 购物。
- 娱乐。
- 生活。
- 其他。

固定收入分类：

- 生活费。
- 兼职。
- 奖学金。
- 红包。
- 退款。
- 其他收入。

自定义分类规则：

- 收入和支出分别维护自己的分类列表。
- 自定义分类名称会去除首尾空格，并合并连续空格。
- 不能与同类型的固定分类或自定义分类重名。
- 固定分类不能改名或删除。
- 删除自定义分类前，必须选择一个同类型的替代分类。
- 删除分类后，历史记录保留，只把分类改为替代分类。

## 8. 浏览器旧数据迁移

浏览器旧版数据来源：

```text
student-expense-tracker:v1
student-finance-tracker:v2
student-finance-tracker:custom-categories:v1
```

迁移规则：

- 优先读取浏览器中的 v2 收支数据。
- 如果 v2 不存在，再读取 v1 支出数据。
- v1 中的每条支出记录会补上 `type: "expense"`，转换为 `Transaction`。
- 同时读取旧版浏览器中的自定义分类数据。
- 迁移请求发送到 `/api/migrate/local-storage`。
- API 只在本机数据库还没有完成迁移时执行导入，避免刷新页面时重复插入。
- 数据库已有相同 `id` 的记录时不会重复插入。
- 迁移成功后，浏览器中的旧数据不会主动删除，便于恢复和排查问题。
- 浏览器数据缺失、格式错误或 JSON 解析失败时，页面不能崩溃。
- 迁移失败时，页面显示温和提示，原浏览器数据不被修改。

迁移完成后，本机数据库是当前应用的主要数据来源。浏览器 localStorage 不再作为正常新增、删除操作的主要存储位置。

## 9. 统计规则

所有统计默认按当前选择月份计算：

- 本月收入：当前月份所有 `type === "income"` 记录金额总和。
- 本月支出：当前月份所有 `type === "expense"` 记录金额总和。
- 本月结余：本月收入 - 本月支出。
- 支出分类统计：当前月份支出记录按分类汇总。
- 收入分类统计：当前月份收入记录按分类汇总。
- 分类占比：某分类金额 / 当前类型总金额。

结余可以为正数、0 或负数。

## 10. 分类详情页

分类详情页路由：

```text
/categories/:type/:category
```

页面显示：

- 分类名称。
- 收支类型：收入或支出。
- 当前筛选月份。
- 该分类在当前月份的总金额。
- 该分类在当前月份的记录笔数。
- 该分类的全部记录列表。
- 每条记录的金额、日期和备注。
- 返回分类统计页的入口。

如果分类不存在、类型错误或当前月份没有记录，要显示清楚的空状态，不能崩溃。

## 11. 收支明细页筛选

收支明细页 `/transactions` 支持：

- 按月份筛选。
- 按类型筛选：全部、收入、支出。
- 按分类筛选。

当类型选择“全部”时，分类下拉框显示收入和支出的全部分类；选择收入或支出后，只显示对应类型的分类，并重置之前的分类筛选。

## 12. 当前开发阶段状态

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| A | 路由、应用框架、导航 | 已完成 |
| B | `Transaction` 模型和旧数据迁移 | 已完成，迁移目标为本机数据库 |
| C | 收入和支出新增入口 | 已完成，使用侧边抽屉 |
| D | 财务概览页 | 已完成 |
| E | 收支明细页和筛选 | 已完成 |
| F | 分类统计、分类详情、自定义分类管理 | 已完成 |
| G | 体验整理、文档和完整验收 | 持续完善 |

后续每次开发只完成一个小阶段，优先从体验问题、数据安全和容易验证的改进开始。

## 13. 协作规则

- 每次开发前先阅读本文件。
- 一次只完成一个小阶段。
- 修改前先检查现有代码和相关文件。
- 使用适合新手维护的简单方案。
- 不擅自增加本文件之外的大功能。
- 新增依赖前先说明用途和必要性。
- 数据存储、账户体系、云同步等会影响架构的选择，必须先列出方案并等待用户确认。
- 每次修改后运行相关测试、检查或构建命令。
- 不覆盖用户已有且与当前任务无关的修改。
- 不确定需求时先询问，不自行做影响产品方向的决定。
- 用简单中文汇报：修改了什么、如何验证、还有什么问题。
