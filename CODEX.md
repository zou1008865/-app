# CODEX.md

## 1. 项目目标

本项目升级为面向大学生个人使用的人民币收支管理 App。

新的第一版目标是：记录支出和收入，查看本月收入、本月支出、本月结余，按分类理解钱从哪里来、花到哪里去，并能查看完整收支明细。

## 2. 当前重要变化

项目从“单页面支出记录工具”升级为“多页面个人收支管理 App”。

新增支持：

- 支出记录。
- 收入记录。
- 本月收入。
- 本月支出。
- 本月结余。
- 分类统计。
- 分类详情页面。
- 收支明细页面。
- 按月份、收入/支出、分类筛选。
- 电脑端使用清楚的侧边导航或顶部导航，不再把全部功能挤在一个页面。

暂时不支持：

- 账户。
- 银行卡。
- 钱包。
- 初始余额。
- 预算。
- 登录。
- 云同步。
- 自定义分类。

这些暂不支持的能力以后如果要增加，必须先列方案并等待用户确认。

## 3. 技术栈方案

已采用技术：

| 技术 | 新手解释 | 用途 |
| --- | --- | --- |
| React | 搭建网页界面的工具 | 制作页面、表单、列表、统计区域 |
| Vite | 项目启动和打包工具 | 本地预览和正式构建 |
| TypeScript | 更严格的 JavaScript | 减少数据字段写错 |
| CSS | 控制页面样式 | 实现校园清新、现代效率工具风格 |
| localStorage | 浏览器本地存储 | 保存用户收支记录 |

### React Router 对比手写页面状态

| 方案 | 适合情况 | 优点 | 缺点 | Codex 建议 |
| --- | --- | --- | --- | --- |
| React Router | 有多个页面和可分享地址 | 路由清楚；支持 `/transactions`、`/categories`；刷新页面后仍在当前页面；后续扩展更自然 | 需要新增依赖 | **推荐** |
| 手写页面状态 | 只有一两个简单页面 | 不新增依赖；实现很快 | 地址不会变化；刷新会回首页；页面多了以后代码容易乱 | 不推荐 |

新增 React Router 依赖的用途：

- 让 App 根据网址显示不同页面。
- 支持 `/`、`/transactions`、`/categories`、`/categories/:type/:category`。
- 让用户刷新页面后仍留在当前页面。
- 让后续新增页面时结构更清楚。

影响：

- 需要新增 `react-router-dom` 依赖。
- 需要调整入口和页面结构。
- 不改变 localStorage 数据本身。
- 不产生直接费用。

## 4. 新页面结构和导航

建议使用电脑端侧边导航，窄屏下改为顶部导航。

原因：

- 页面从单页变多后，侧边导航更清楚。
- 电脑端空间足够，侧边导航不会挤压主要内容。
- 手机端屏幕窄，顶部导航更自然。

建议路由：

| 路由 | 页面 | 主要内容 |
| --- | --- | --- |
| `/` | 财务概览 | 本月收入、本月支出、本月结余、最近记录、简要分类统计 |
| `/transactions` | 收支明细 | 全部记录列表，支持月份、收入/支出、分类筛选 |
| `/categories` | 分类统计 | 按收入和支出分别展示分类金额与占比 |
| `/categories/:type/:category` | 分类详情 | 查看某个收入或支出分类下的全部记录 |

## 5. 新增记录入口方案

| 方案 | 适合情况 | 优点 | 缺点 | Codex 建议 |
| --- | --- | --- | --- | --- |
| 弹窗 | 表单短、希望快速记一笔 | 不离开当前页面；体验轻便 | 小屏幕上空间紧张；复杂表单不舒服 | 可选 |
| 侧边抽屉 | 电脑端使用为主，想保持上下文 | 不遮挡主页面太多；适合现代效率工具 | 手机端需要改成底部/全屏样式 | **推荐** |
| 独立页面 | 表单复杂或需要很多步骤 | 结构最清楚；手机端也稳定 | 新增一笔要离开当前页面，速度略慢 | 后续可考虑 |

推荐：第一版使用“侧边抽屉”。电脑端从右侧滑出，手机端可以退化成全屏面板或普通页面样式。它适合“校园手账风 + 现代效率工具”的方向，也不会让首页过于拥挤。

## 6. 分类设计

支出分类：

- 餐饮。
- 交通。
- 学习。
- 购物。
- 娱乐。
- 生活。
- 其他。

收入分类：

- 生活费。
- 兼职。
- 奖学金。
- 红包。
- 退款。
- 其他收入。

第一版不支持自定义分类。

## 7. Transaction 数据模型

将原来的 `Expense` 升级为 `Transaction`。

| 字段 | 含义 | 规则 |
| --- | --- | --- |
| `id` | 记录唯一标识 | 新增时生成，不能重复 |
| `type` | 收支类型 | `expense` 或 `income` |
| `amountInCents` | 金额，单位是分 | 必须是大于 0 的整数 |
| `category` | 分类 | 必须属于对应类型的固定分类 |
| `note` | 备注 | 可以为空 |
| `date` | 发生日期 | 新增时默认今天，用户可以修改 |
| `createdAt` | 创建时间 | 新增记录时自动生成 |

金额规则：

- 输入时最多允许两位小数，例如 `12`、`12.5`、`12.50`。
- 保存时必须转成整数“分”，例如 `12.50` 保存为 `1250`。
- 金额必须大于 0。
- 不直接用小数保存金额。

## 8. localStorage v1 到 v2 迁移方案

现有 v1 存储键：

```text
student-expense-tracker:v1
```

现有 v1 数据结构：

```json
{
  "version": 1,
  "expenses": []
}
```

新增 v2 存储键：

```text
student-finance-tracker:v2
```

新增 v2 数据结构：

```json
{
  "version": 2,
  "transactions": []
}
```

迁移规则：

- 启动时优先读取 v2。
- 如果 v2 存在且格式正确，直接使用 v2。
- 如果 v2 不存在，再尝试读取 v1。
- 如果 v1 存在且格式正确，把每条 `Expense` 转成 `Transaction`：
  - `type` 固定为 `expense`。
  - `amountInCents` 保留。
  - `category` 保留。
  - `note` 保留。
  - `date` 保留。
  - `createdAt` 保留。
- 迁移成功后写入 v2。
- 不主动删除 v1，避免误删用户旧数据。
- v1 或 v2 数据缺失、格式错误、JSON 解析失败时，页面不能崩溃。
- 如果读取失败，显示空账本和温和提示。

## 9. 计算规则

所有统计默认按当前选择月份计算。

- 本月收入：当前月份所有 `type === "income"` 记录金额总和。
- 本月支出：当前月份所有 `type === "expense"` 记录金额总和。
- 本月结余：本月收入 - 本月支出。
- 支出分类统计：当前月份支出记录按支出分类汇总。
- 收入分类统计：当前月份收入记录按收入分类汇总。
- 分类占比：某分类金额 / 当前类型总金额。

结余可以为正数、0 或负数。

## 10. 分类详情页内容

分类详情页路由：

```text
/categories/:type/:category
```

页面需要显示：

- 分类名称。
- 收支类型：收入或支出。
- 当前筛选月份。
- 该分类在当前月份的总金额。
- 该分类在当前月份的记录笔数。
- 该分类的全部记录列表。
- 每条记录显示金额、日期、备注。
- 可以返回分类统计页。

如果分类不存在、类型错误或当前月份没有记录，要显示清楚的空状态，不能崩溃。

## 11. 收支明细页筛选

收支明细页 `/transactions` 需要支持：

- 按月份筛选。
- 按类型筛选：全部、收入、支出。
- 按分类筛选。

分类筛选规则：

- 类型为“全部”时，可以显示全部收入和支出分类，或先选择类型再选择分类。
- 为了新手体验，推荐先做“选择类型后再显示对应分类”。

## 12. 后续开发阶段

### 阶段 A：引入路由和应用框架

要实现：

- 新增 React Router。
- 建立页面路由。
- 增加电脑端侧边导航和窄屏顶部导航。
- 先保持现有功能可用。

验收：

- `/`、`/transactions`、`/categories` 可以打开。
- 刷新页面后仍停留在当前路由。
- 不丢失现有 localStorage v1 数据。

### 阶段 B：升级数据模型和迁移

要实现：

- `Expense` 升级为 `Transaction`。
- 新增收入/支出类型。
- 实现 localStorage v1 到 v2 迁移。
- 保留旧支出记录。

验收：

- 旧 v1 支出能迁移为 v2 支出。
- v2 数据包含 `version: 2` 和 `transactions`。
- 异常数据不导致页面崩溃。

### 阶段 C：新增收入和支出记录入口

要实现：

- 用侧边抽屉新增记录。
- 支持选择收入或支出。
- 根据类型显示对应分类。
- 金额、日期、备注校验沿用现有规则。

验收：

- 可以新增收入。
- 可以新增支出。
- 收入分类和支出分类不混用。
- 刷新后记录保留。

### 阶段 D：财务概览页

要实现：

- 显示本月收入。
- 显示本月支出。
- 显示本月结余。
- 显示最近记录。
- 显示简要分类统计。

验收：

- 本月收入、本月支出、本月结余计算正确。
- 结余可以显示负数。
- 非当前月份记录不计入本月统计。

### 阶段 E：收支明细页

要实现：

- 全部记录列表。
- 月份筛选。
- 收入/支出筛选。
- 分类筛选。

验收：

- 筛选结果正确。
- 没有结果时显示空状态。
- 删除记录后筛选和统计同步更新。

### 阶段 F：分类统计和分类详情

要实现：

- 收入分类统计。
- 支出分类统计。
- 分类详情页。
- 点击分类进入详情。

验收：

- 分类金额和占比正确。
- 分类详情只显示对应类型和分类的记录。
- 错误路由不崩溃。

### 阶段 G：体验整理和文档更新

要实现：

- 优化电脑端和手机端体验。
- 更新 README。
- 完整构建检查。
- 手动验收清单更新为收支版本。

验收：

- `npm run build` 成功。
- 主要流程可用：新增收入、新增支出、查看概览、筛选明细、查看分类详情、刷新保留。

## 13. 协作规则

- 每次开发前先阅读本文件。
- 一次只完成一个小阶段。
- 修改前先检查现有代码。
- 使用适合新手维护的简单方案。
- 不擅自增加本文件之外的大功能。
- 新增依赖前先说明用途和必要性。
- 每次修改后运行相关测试、检查或构建命令。
- 不覆盖用户已有且与当前任务无关的修改。
- 不确定需求时先询问，不自行做影响产品方向的决定。
- 用简单中文汇报：修改了什么、如何验证、还有什么问题。

